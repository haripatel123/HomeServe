<%- include('partials/header') %>
    <%- include('partials/navbar') %>

        <!-- HERO -->
        <section class="hero-section">
            <div class="container-xl position-relative" style="z-index:1">
                <div class="row align-items-center">
                    <div class="col-lg-7">
                        <h1 class="hero-title">
                            Find Trusted Professionals<br>
                            For Every Home Need
                        </h1>
                        <p class="hero-subtitle">
                            Find trusted experts for repairs, maintenance, and home
                            improvements with transparent pricing and verified reviews.
                        </p>
                        <div class="d-flex gap-3 flex-wrap ">
                            <a href="#services" class="btn-green">
                                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
                                    stroke="currentColor" stroke-width="2" style="width:16px;height:16px;">
                                    <circle cx="11" cy="11" r="8" />
                                    <line x1="21" y1="21" x2="16.65" y2="16.65" />
                                </svg>
                                Browse Services
                            </a>
                            <% if (locals.user) { %>
                                <% if (user.role === 'customer') { %>
                                    <a href="/bookings" class="btn-outline-custom">My Bookings</a>
                                <% } else if (user.role === 'provider') { %>
                                    <a href="/provider" class="btn-outline-custom">My Dashboard</a>
                                <% } else if (user.role === 'admin') { %>
                                    <a href="/analytics" class="btn-outline-custom">Analytics</a>
                                <% } %>
                            <% } else { %>
                                <a href="/login" class="btn-outline-custom">Login to Book</a>
                            <% } %>
                        </div>

                        <div class="hero-stats">
                            <div class="hero-stat-item">
                                <div class="stat-number">20+</div>
                                <div class="stat-label">Services</div>
                            </div>
                            <div class="hero-stat-item">
                                <div class="stat-number">6</div>
                                <div class="stat-label">Providers</div>
                            </div>
                            <div class="hero-stat-item">
                                <div class="stat-number">4.7★</div>
                                <div class="stat-label">Avg Rating</div>
                            </div>
                            <div class="hero-stat-item">
                                <div class="stat-number">40+</div>
                                <div class="stat-label">Bookings</div>
                            </div>
                        </div>
                    </div>

                    <div class="col-lg-5 col-12 d-flex justify-content-center justify-content-lg-end mt-5 mt-lg-0">
                        <div class="hero-feed-container">
                            <div class="hero-feed-header d-flex justify-content-between align-items-center">
                                <div class="d-flex align-items-center gap-2">
                                    <div class="hero-feed-pulse"></div>
                                    <span class="hero-feed-title">Recent Bookings</span>
                                </div>
                                <% if (locals.featuredProvider) { %>
                                <span class="badge-status badge-completed" style="font-size:0.7rem;padding:2px 8px;">Top Pro: <%= featuredProvider.name %></span>
                                <% } %>
                            </div>
                            <div class="hero-feed-scroll" id="heroFeedScroll">
                                <div class="hero-feed-item">
                                    <div class="hero-feed-avatar" style="background:rgba(59,130,246,0.1);color:#3b82f6;">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="20" height="20"><path d="M5.636 18.364a9 9 0 0 1 0-12.728"/><path d="M3.172 3.172C-.77 7.114-.77 13.486 3.172 17.828"/><path d="M12 12v.01"/><path d="M12 2a4 4 0 0 0-4 4c0 1.5.8 3 2 4l2 2 2-2c1.2-1 2-2.5 2-4a4 4 0 0 0-4-4z"/></svg>
                                    </div>
                                    <div class="hero-feed-info">
                                        <div class="hero-feed-name">Priya Shah</div>
                                        <div class="hero-feed-service">Booked Plumbing Repair</div>
                                    </div>
                                    <div class="hero-feed-time">2m ago</div>
                                    <div class="hero-feed-dot"></div>
                                </div>
                                <div class="hero-feed-item">
                                    <div class="hero-feed-avatar" style="background:rgba(251,191,36,0.1);color:#fbbf24;">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="20" height="20"><path d="M13 2 3 14h9l-1 8 10-12h-9l1-8z"/></svg>
                                    </div>
                                    <div class="hero-feed-info">
                                        <div class="hero-feed-name">Amit Patel</div>
                                        <div class="hero-feed-service">Booked Electrical Wiring</div>
                                    </div>
                                    <div class="hero-feed-time">5m ago</div>
                                    <div class="hero-feed-dot"></div>
                                </div>
                                <div class="hero-feed-item">
                                    <div class="hero-feed-avatar" style="background:rgba(52,211,153,0.1);color:#34d399;">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="20" height="20"><path d="M3 22v-2a4 4 0 0 1 4-4h2"/><circle cx="12" cy="5" r="3"/><path d="m15 9 3 3-3 3"/><path d="M18 12H9"/></svg>
                                    </div>
                                    <div class="hero-feed-info">
                                        <div class="hero-feed-name">Neha Gupta</div>
                                        <div class="hero-feed-service">Booked Deep Cleaning</div>
                                    </div>
                                    <div class="hero-feed-time">12m ago</div>
                                    <div class="hero-feed-dot"></div>
                                </div>
                                <div class="hero-feed-item">
                                    <div class="hero-feed-avatar" style="background:rgba(168,85,247,0.1);color:#a855f7;">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="20" height="20"><path d="M12 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.375 2.625a1 1 0 0 1 3 3l-9.013 9.014a2 2 0 0 1-.853.505l-2.873.84a.5.5 0 0 1-.62-.62l.84-2.873a2 2 0 0 1 .506-.852z"/></svg>
                                    </div>
                                    <div class="hero-feed-info">
                                        <div class="hero-feed-name">Rahul Verma</div>
                                        <div class="hero-feed-service">Booked Furniture Assembly</div>
                                    </div>
                                    <div class="hero-feed-time">18m ago</div>
                                    <div class="hero-feed-dot"></div>
                                </div>
                                <div class="hero-feed-item">
                                    <div class="hero-feed-avatar" style="background:rgba(251,113,133,0.1);color:#fb7185;">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="20" height="20"><path d="m2 22 1-1h3l9-9"/><path d="M3 21v-3l9-9"/><path d="m15 6 3.4-3.4a2.1 2.1 0 1 1 3 3L18 9l.4.4a2.1 2.1 0 1 1-3 3l-3.8-3.8a2.1 2.1 0 1 1 3-3l.4.4z"/></svg>
                                    </div>
                                    <div class="hero-feed-info">
                                        <div class="hero-feed-name">Sneha Iyer</div>
                                        <div class="hero-feed-service">Booked Wall Painting</div>
                                    </div>
                                    <div class="hero-feed-time">25m ago</div>
                                    <div class="hero-feed-dot"></div>
                                </div>
                                <div class="hero-feed-item">
                                    <div class="hero-feed-avatar" style="background:rgba(251,146,60,0.1);color:#fb923c;">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="20" height="20"><path d="M17.7 7.7a2.5 2.5 0 1 1 1.8 4.3H2"/><path d="M9.6 4.6A2 2 0 1 1 11 8H2"/><path d="M12.6 19.4A2 2 0 1 0 14 16H2"/></svg>
                                    </div>
                                    <div class="hero-feed-info">
                                        <div class="hero-feed-name">Karan Mehta</div>
                                        <div class="hero-feed-service">Booked AC Servicing</div>
                                    </div>
                                    <div class="hero-feed-time">31m ago</div>
                                    <div class="hero-feed-dot"></div>
                                </div>
                                <div class="hero-feed-item">
                                    <div class="hero-feed-avatar" style="background:rgba(20,184,166,0.1);color:#14b8a6;">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="20" height="20"><path d="M7 16.3c2.2 0 4-1.83 4-4.05 0-1.16-.57-2.26-1.71-3.19S7.29 6.75 7 5.3c-.29 1.45-1.14 2.84-2.29 3.76S3 11.1 3 12.25c0 2.22 1.8 4.05 4 4.05z"/><path d="M12.56 14.06c1.32 0 2.44-1.1 2.44-2.44 0-.7-.34-1.36-1.02-1.92-.69-.56-1.18-1.14-1.42-1.9-.23.76-.72 1.34-1.42 1.9-.68.56-1.02 1.22-1.02 1.92 0 1.34 1.1 2.44 2.44 2.44z"/></svg>
                                    </div>
                                    <div class="hero-feed-info">
                                        <div class="hero-feed-name">Divya Rao</div>
                                        <div class="hero-feed-service">Booked Water Purifier Service</div>
                                    </div>
                                    <div class="hero-feed-time">40m ago</div>
                                    <div class="hero-feed-dot"></div>
                                </div>
                                <div class="hero-feed-item">
                                    <div class="hero-feed-avatar" style="background:rgba(139,92,246,0.1);color:#8b5cf6;">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="20" height="20"><rect width="7" height="13" x="3" y="8" rx="1"/><path d="m15 2-3 6h4l-3 6"/><path d="M14 8h4a2 2 0 0 1 2 2v6.5a.5.5 0 0 1-.5.5h-5"/></svg>
                                    </div>
                                    <div class="hero-feed-info">
                                        <div class="hero-feed-name">Vikram Singh</div>
                                        <div class="hero-feed-service">Booked Appliance Repair</div>
                                    </div>
                                    <div class="hero-feed-time">52m ago</div>
                                    <div class="hero-feed-dot"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- FILTER BAR -->
        <div class="filter-bar">
            <div class="container-xl">
                <form method="GET" action="/" class="d-flex align-items-center gap-2 flex-wrap">
                    <!-- Search -->
                    <div style="flex:1;min-width:200px;">
                        <div class="search-input-group">
                            <input type="text" name="search" value="<%= searchQuery %>" id="searchInput" placeholder=" " autocomplete="off">
                            <label for="searchInput">Search services...</label>
                        </div>
                    </div>
                    <!-- Category buttons -->
                    <div class="d-flex gap-2 flex-wrap align-items-center">
                        <a href="/" class="category-btn <%=!selectedCategory?'active':''%>">All</a>
                        <% categories.forEach(cat=> { %>
                            <a href="/?category=<%= cat.category_id %>"
                                class="category-btn <%=selectedCategory===cat.category_id?'active':''%>">
                                <%= cat.name %>
                            </a>
                            <% }); %>
                    </div>
                </form>
            </div>
        </div>

        <!-- SERVICES GRID -->
        <div class="page-wrapper" id="services">
            <div class="container-xl">
                <!-- Section heading -->
                <div class="d-flex align-items-center justify-content-between mb-4">
                    <h2 class="section-heading mb-0">
                        <% if (selectedCategory) { %>
                            <%= categories.find(c=>c.category_id===selectedCategory)?.name || 'Services' %>
                                <% } else { %>
                                    All Services
                                    <% } %>
                    </h2>
                    <span class="text-muted-custom" style="font-size:0.85rem;">
                        <%= services.length %> service<%= services.length !==1 ? 's' : '' %> found
                    </span>
                </div>

                <% if (services.length===0) { %>
                    <div class="empty-state">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                            stroke-width="1.5">
                            <circle cx="11" cy="11" r="8" />
                            <line x1="21" y1="21" x2="16.65" y2="16.65" />
                        </svg>
                        <h3>No Services Found</h3>
                        <p>Try a different category or search term.</p>
                        <a href="/" class="btn-green mt-3">View All Services</a>
                    </div>
                    <% } else { %>

                        <% 
                        // Group services by category 
                        const grouped={}; 
                        services.forEach(s=> {
                            if (!grouped[s.category_name]) grouped[s.category_name] = [];
                            grouped[s.category_name].push(s);
                            });
                            const categoryNames = Object.keys(grouped);

                            const svgIcons = {
                            'Plumbing': `
                            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" />
                            <polyline points="9 22 9 12 15 12 15 22" />`,
                            'Electrical': `
                            <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" />`,
                            'Cleaning': `
                            <path
                                d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z" />
                            `,
                            'Carpentry': `
                            <rect x="3" y="3" width="18" height="18" rx="2" ry="2" />
                            <line x1="3" y1="9" x2="21" y2="9" />
                            <line x1="3" y1="15" x2="21" y2="15" />`,
                            'Painting': `
                            <path d="M19 11H5a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-6a2 2 0 0 0-2-2z" />
                            <path d="M12 3v8" />`,
                            'AC & Appliances': `
                            <circle cx="12" cy="12" r="10" />
                            <line x1="12" y1="8" x2="12" y2="12" />
                            <line x1="12" y1="16" x2="12.01" y2="16" />`,
                            'Pest Control': `
                            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />`,
                            'Gardening': `
                            <path
                                d="M17 8C8 10 5.9 16.17 3.82 21.34L5.71 22l1-2.3A4.49 4.49 0 0 0 8 20c9.13 0 13.84-7.83 14-8l-5-4z" />
                            `,
                            };

                            const serviceImages = {
                            'Pipe Leak Repair': 'Pipe Leak Repair.avif',
                            'Tap / Faucet Installation': 'Tap Installation.webp',
                            'Drain Cleaning': 'Drain Cleaning.webp',
                            'Switchboard Repair': 'Switch board Repair.webp',
                            'Fan Installation': 'Fan Installation.webp',
                            'Inverter / UPS Setup': 'Inverter Setup.avif',
                            'Home Deep Cleaning': 'Home Deep Cleaning.webp',
                            'Sofa / Carpet Cleaning': 'Sofa and Carpet Cleaning.webp',
                            'Kitchen Cleaning': 'Kitchen Cleaning.webp',
                            'Furniture Assembly': 'Furniture Assembly.webp',
                            'Door Repair / Fitting': 'Door Repair.avif',
                            'Wall Painting': 'Wall Painting.webp',
                            'Touch-Up Painting': 'Touch-Up Painting.webp',
                            'AC Service & Cleaning': 'AC Service.avif',
                            'Refrigerator Repair': 'Referigerator Service.webp',
                            'Washing Machine Repair': 'Waching Machine.webp',
                            'Cockroach Treatment': 'Coacroch Treatment.avif',
                            'Termite Treatment': 'Termite Treatment.webp',
                            'Lawn Mowing & Trimming': 'Lawn Cleaning.webp',
                            'Plant Care Package': 'Plant Care.webp'
                            };
                            %>

                            <% categoryNames.forEach(catName=> { %>
                                <div class="mb-5">
                                    <% if (!selectedCategory) { %>
                                        <h3
                                            style="font-size:1rem;font-weight:600;color:var(--text-secondary);text-transform:uppercase;letter-spacing:0.8px;margin-bottom:1rem;display:flex;align-items:center;gap:0.5rem;">
                                            <span
                                                style="width:3px;height:16px;background:var(--accent-green);border-radius:2px;display:inline-block;"></span>
                                            <%= catName %>
                                        </h3>
                                        <% } %>
                                            <div class="row g-3">
                                                <% grouped[catName].forEach((service, idx)=> {
                                                    let displayPrice = service.base_price;
                                                    if (service.min_price) displayPrice = service.min_price;
                                                     const iconSvg = svgIcons[catName] || `<circle cx="12" cy="12" r="10" />`;
                                                     const imgFile = serviceImages[service.service_name] ||
                                                     serviceImages[service.name];
                                                    %>
                                                    <div class="col-6 col-md-4 col-lg-3">
                                                        <a href="/services/<%= service.service_id %>"
                                                            style="text-decoration:none;display:block;height:100%;">
                                                            <div class="service-card h-100">
                                                                <div class="service-card-image">
                                                                    <% if (imgFile) { %>
                                                                        <img src="/images/categories/<%= imgFile %>"
                                                                            alt="<%= service.service_name %>"
                                                                            class="img-fluid service-card-img"
                                                                            loading="lazy">
                                                                        <% } %>
                                                                            <div class="service-card-icon-fallback"
                                                                                style="<%= imgFile ? 'display:none;' : '' %>">
                                                                                <svg xmlns="http://www.w3.org/2000/svg"
                                                                                    viewBox="0 0 24 24" fill="none"
                                                                                    stroke="currentColor"
                                                                                    stroke-width="1.5">
                                                                                    <%- iconSvg %>
                                                                                </svg>
                                                                            </div>
                                                                </div>
                                                                <div class="service-card-body">
                                                                    <div class="service-card-category">
                                                                        <%= service.category_name %>
                                                                    </div>
                                                                    <div class="service-card-title">
                                                                        <%= service.service_name %>
                                                                    </div>
                                                                    <div class="service-card-desc">
                                                                        <%= service.description %>
                                                                    </div>
                                                                </div>
                                                                <div class="service-card-footer">
                                                                    <div>
                                                                        <div class="service-price">
                                                                            ₹<%= parseFloat(displayPrice).toLocaleString('en-IN')
                                                                                %>
                                                                                <% if (service.variant_count> 0) {
                                                                                    %><span
                                                                                        style="font-size:0.7rem;color:var(--text-muted);">
                                                                                        onwards</span>
                                                                                    <% } %>
                                                                        </div>
                                                                    </div>
                                                                    <div class="service-duration">
                                                                        <svg xmlns="http://www.w3.org/2000/svg"
                                                                            viewBox="0 0 24 24" fill="none"
                                                                            stroke="currentColor" stroke-width="2"
                                                                            style="width:12px;height:12px;margin-right:3px;">
                                                                            <circle cx="12" cy="12" r="10" />
                                                                            <polyline points="12 6 12 12 16 14" />
                                                                        </svg>
                                                                        <%= service.duration_min %> min
                                                                    </div>
                                                                </div>
                                                            </div>
                                                        </a>
                                                    </div>
                                                    <% }); %>
                                            </div>
                                </div>
                                <% }); %>
                                    <% } %>
            </div>
        </div>

        <!-- GSAP Core Library -->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>

        <%- include('partials/footer') %>

            <script>
                // Dynamic Category Filtering (AJAX Page Swapping without Page Refresh or Scroll Jumps)
                document.addEventListener('click', function (e) {
                    const categoryBtn = e.target.closest('.category-btn');
                    const heroCard = e.target.closest('.hero-category-card');
                    
                    if (categoryBtn || heroCard) {
                        e.preventDefault();
                        const targetEl = categoryBtn || heroCard;
                        const url = targetEl.getAttribute('href');

                        // 1. Start scrolling IMMEDIATELY upon click for instant feedback
                        if (heroCard) {
                            const servicesEl = document.getElementById('services');
                            if (servicesEl) {
                                const elementPosition = servicesEl.getBoundingClientRect().top + window.pageYOffset;
                                const offsetPosition = elementPosition - 125; // comfortable eyesight alignment

                                const startY = window.pageYOffset;
                                const difference = offsetPosition - startY;
                                const duration = 75; // snappy, smooth scroll duration (450ms)
                                let startTime = null;

                                function step(timestamp) {
                                    if (!startTime) startTime = timestamp;
                                    const progress = timestamp - startTime;
                                    const percent = Math.min(progress / duration, 1);
                                    const ease = percent < 0.5 
                                        ? 2 * percent * percent 
                                        : -1 + (4 - 2 * percent) * percent;

                                    window.scrollTo(0, startY + difference * ease);

                                    if (progress < duration) {
                                        window.requestAnimationFrame(step);
                                    }
                                }
                                window.requestAnimationFrame(step);
                            }
                        }

                        // 2. Fetch and swap the DOM content in the background in parallel
                        fetch(url)
                            .then(res => res.text())
                            .then(html => {
                                const parser = new DOMParser();
                                const doc = parser.parseFromString(html, 'text/html');

                                // Replace the services container content
                                const newServices = doc.getElementById('services');
                                const oldServices = document.getElementById('services');
                                if (newServices && oldServices) {
                                    oldServices.innerHTML = newServices.innerHTML;
                                }

                                // Replace the filter bar (to update active button state and keep search term)
                                const newFilter = doc.querySelector('.filter-bar');
                                const oldFilter = document.querySelector('.filter-bar');
                                if (newFilter && oldFilter) {
                                    oldFilter.innerHTML = newFilter.innerHTML;
                                }

                                // Update the browser address bar URL without reloading
                                history.pushState({}, '', url);
                            })
                            .catch(err => {
                                // Fallback to traditional refresh if fetch fails
                                if (!heroCard) window.location.href = url;
                            });
                    }
                });

                // Live Client-Side Search (Event-Delegated so it works on dynamically loaded cards)
                document.addEventListener('input', function (e) {
                    if (e.target.id === 'searchInput') {
                        const q = e.target.value.toLowerCase().trim();
                        document.querySelectorAll('.service-card').forEach(card => {
                            const title = card.querySelector('.service-card-title')?.textContent.toLowerCase() || '';
                            const desc = card.querySelector('.service-card-desc')?.textContent.toLowerCase() || '';
                            const cat = card.querySelector('.service-card-category')?.textContent.toLowerCase() || '';
                            const col = card.closest('.col-6, .col-md-4, .col-lg-3');
                            if (col) col.style.display = (!q || title.includes(q) || desc.includes(q) || cat.includes(q)) ? '' : 'none';
                        });
                    }
                });

                // Mobile menu toggle
                document.addEventListener('click', function (e) {
                    const toggle = e.target.closest('#mobileMenuToggle');
                    if (toggle) {
                        const menu = document.getElementById('mobileMenu');
                        if (menu) {
                            menu.style.display = menu.style.display === 'none' || menu.style.display === '' ? 'block' : 'none';
                        }
                    }
                });

                // CSP compliant image loading error handling
                document.addEventListener('error', function (e) {
                    if (e.target.tagName === 'IMG' && e.target.classList.contains('service-card-img')) {
                        e.target.style.display = 'none';
                        const fallback = e.target.parentNode.querySelector('.service-card-icon-fallback');
                        if (fallback) {
                            fallback.style.display = 'flex';
                        }
                    }
                }, true); // capturing phase because error events don't bubble

                // ── Live Booking Feed — infinite scroll + pause on hover ──
                document.addEventListener('DOMContentLoaded', () => {
                    const feed = document.getElementById('heroFeedScroll');
                    if (!feed) return;
                    // Duplicate items for seamless infinite scroll
                    feed.innerHTML += feed.innerHTML;
                    // Pause on hover
                    const container = feed.closest('.hero-feed-container');
                    if (container) {
                        container.addEventListener('mouseenter', () => feed.style.animationPlayState = 'paused');
                        container.addEventListener('mouseleave', () => feed.style.animationPlayState = 'running');
                    }
                });
            </script>